// Set this to your deployed backend's URL after you deploy it
// (e.g. "https://your-backend.onrender.com"). Leave as-is for local testing
// against "uvicorn api.app:app --port 8000" running on your own machine.
const BACKEND_URL = "http://localhost:8000";
