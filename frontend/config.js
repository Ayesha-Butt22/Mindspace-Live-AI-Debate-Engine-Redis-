// Deployed backend (Render). For local testing against
// "uvicorn api.app:app --port 8000", temporarily change this back to
// "http://localhost:8000".
const BACKEND_URL = "https://redis-ai-chat-backend.onrender.com";
